name: locked_in
description: LOCKED IN - Fitness & Life Lock-in App. Track workouts, habits, study, and dominate with your squad.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  http: ^1.2.0
  shared_preferences: ^2.2.2
  sqflite: ^2.3.2
  sqflite_common_ffi_web: ^1.1.2
  path: ^1.8.3
  path_provider: ^2.1.2
  intl: ^0.19.0
  google_fonts: ^6.1.0
  flutter_animate: ^4.3.0
  confetti: ^0.7.0
  percent_indicator: ^4.2.3
  fl_chart: ^0.66.0
  flutter_svg: ^2.0.9
  uuid: ^4.2.2
  qr_flutter: ^4.1.0
  flutter_local_notifications: ^17.2.1
  timezone: ^0.9.4
  flutter_timezone: ^4.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
